# Em produção o frontend é servido pelo mesmo domínio do backend (via nginx),
# então /api é path relativo — não precisa setar host.
# Se um dia separar (frontend em outro domínio), troque para:
#   VITE_API_URL=https://api.seudominio.com
VITE_API_URL=
